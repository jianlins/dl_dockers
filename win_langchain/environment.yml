name: win_llm
channels:
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - jupyterlab
  - pip:
    - pyspark==3.4.2
    - spark-nlp==5.3.3  
    - pyarrow
    - findspark
    - spark-nlp-display
    

